import random

random.randint(a,b)


#you can use this to get a number between any two number .
#the inital number 'a' is the number which indicates the number of which we need the higher number , any result will be greater than 'a'.
#the 2nd number 'b' indicates the number of which the result needs to be smaller than.
